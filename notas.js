/*
Props: se usa para comunicar los componentes unos con otros
Los componentes se comunican con props y funciones, en un flujo de arriba hacia abajo
estado: es una herramienta de react js. es imutable. 
son datos internos de un componente que pueden cambiar en el tiempo y que, cuando cambian, hacen que la interfaz se vuelva a renderizar automáticamente.

useState: permite guardar y cambiar datos dentro de un componente, y hacer que la pantalla se actualice automáticamente cuando esos datos cambian.
estados derivados 

react no espera una funcion ejecutada se envia una funcion dentro de otra funcion para que react la ejecute al momento de realizar el evento 
*/
